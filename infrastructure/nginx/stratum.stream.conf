upstream stratum_tcp_upstream {
  least_conn;
  server stratum-gateway-1:3333 max_fails=3 fail_timeout=10s;
  server stratum-gateway-2:3333 max_fails=3 fail_timeout=10s;
}

server {
  listen 3333;
  proxy_pass stratum_tcp_upstream;
  proxy_timeout 300s;
  proxy_connect_timeout 5s;
}
